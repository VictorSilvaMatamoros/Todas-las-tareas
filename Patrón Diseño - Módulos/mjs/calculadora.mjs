
// Podemos exportar funciones o valores así:
export function sumar(a, b) {
  return a + b
}

// O así:
export const restar = (a, b) => {
  return a - b;
}

// Y a su vez, podemos exportar ambas conjuntamente con:
//export { sumar, restar }
